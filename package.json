{
    "scripts": {
        "dev": "npm-run-all --parallel dev:client dev:server",
        "dev:client": "cd client && yarn && yarn dev",
        "dev:server": "cd server && yarn && yarn dev"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
